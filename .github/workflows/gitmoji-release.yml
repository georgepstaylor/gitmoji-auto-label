name: Create release

on:
    workflow_dispatch:
        inputs:
            release_type:
                description: 'Type of release'
                required: true
                type: choice
                options:
                    - 'major'
                    - 'minor'
                    - 'patch'

jobs:
    release-job:
        runs-on: ubuntu-latest
        steps:
        - uses: georgepstaylor/gitmoji-release-action@90d2db52fafeaf6b0f78852afd4359d7b1f8d1b6
          with:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            release_type: 'patch'
            auto_create_tag: true
            title_suffix: '- Release'         
